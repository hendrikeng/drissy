{% import "_macros/_macros.twig" as macro %}

<div class="c-nav">
    {% include '_pieces/logo/default'%}
    <nav itemscope="itemscope" itemtype="http://schema.org/SiteNavigationElement">
        <ul class="list-reset flex-grow flex items-x-end w-auto">
            {% for block in opt.data %}
                {% if block.isActive %}
                    {# -- Get Position -- #}
                    {% set segments = block.highlightTriggers | default('notSet') | replace({'home': ''}) | split(',') %}
                    {# -- Set Text -- #}
                    {% set text = block.menuItem.customText != '' ? block.menuItem.customText: block.menuItem.text | default(block.menuItem.url) %}
                    <li>
                        <a href="{{ block.menuItem.url }}" title="{{ text | trim }}" class="no-underline inline-block hover:text-brand text-text {{ block.segmentPosition ? macro.isActive(segments, block.segmentPosition + 0) | trim : ''}}{# { loop.last ? '' : 'mr-4' } #} mr-4">
                            {{ block.menuItem.customText | trim }}</a>
                    </li>
                {% endif %}
            {% endfor %}
        </ul>
    </nav>


</div>
