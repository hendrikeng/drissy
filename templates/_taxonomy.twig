{# @var craft \craft\web\twig\variables\CraftVariable #}
{# @var entry \craft\elements\Entry #}

{#  Handler-Template: Taxonomy
 #
 #  Handles all the request for individual category handles
 #
 #}

{% include [
    'taxonomy/' ~ category.group().handle ~ '/' ~ category.slug,
    'taxonomy/' ~ category.group().handle ~ '/default',
    'taxonomy/default',
    '404'
    ] %}
